import React from 'react'

/**
* @author
* @function Title
**/

const Title = ({ title }) => {
  return(
    <div className="section-title">
        <h4>{title}</h4>
    </div>
   )

 }

export default Title